/**
 * Location API Data Source
 * Contains all location data with translations for all supported languages
 */

import apiData from '../../../data/migration/locations-api-ready.json';

export const locationsData = apiData;

export default locationsData;
